{
  "name": "interview-mate",
  "version": "1.0.0",
  "description": "Interview prep pack generator — vanilla HTML/CSS/JS",
  "scripts": {
    "build": "mkdir -p dist && cp index.html styles.css app.js dist/ && echo 'Build complete'",
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}